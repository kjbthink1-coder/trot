# Google Antigravity 작업 수정 요구서 v2
## 기존 트로트/연예인 뉴스 쇼츠 자동화 시스템 + 축적형 미디어 DB / B-roll 시스템

> **목적:** 현재 정상 작동 중인 프로그램을 최대한 보존하면서, 반복적인 이미지·B-roll 검색을 줄이고 작업할수록 미디어가 축적되어 점점 빨라지는 구조로 확장한다.
>
> **중요:** 이 문서는 현재 Antigravity가 확인한 실제 구현 상태를 기준으로 작성한다. 기존 기능을 재설계하거나 처음부터 다시 만드는 작업이 아니다.

---

# 1. 최우선 개발 원칙

## 1-1. 기존 기능을 절대 깨뜨리지 않는다

현재 정상 작동하는 아래 기능은 **그대로 유지**한다.

- 기사 URL 직접 입력
- 가수별 뉴스 바로가기
- 기사 자동 파싱
- Gemini API 분석
- 5070 타깃 결론 은닉형 썸네일 문구 5개 생성
- 약 60초 쇼츠 대본 생성
- 체류시간을 고려한 네이버 블로그 4파트 글 생성
- 관련 이미지 약 30장 스마트 수집
- 세로형(9:16) 및 얼굴 인식 우선 이미지 수집
- 사용자가 이미지 삭제/선별
- 선택 이미지 중 썸네일 1클릭 지정
- 썸네일 실시간 글자 크기/색상 조정
- 가수 얼굴을 가리지 않도록 썸네일 배치
- 선택 썸네일을 쇼츠 첫 프레임/커버로 연결
- Edge-TTS 남성/여성 음성
- 한 줄 굵은 자막
- 자막이 화면을 과도하게 가리지 않는 기존 배치
- OpenCV 얼굴 중심 스마트 크롭
- 9:16 영상 처리
- **현재 코드의 2.8초 Ken Burns(줌/패닝) 효과**
- 기존 가수 무음 무대 영상 2개 자동 교차 삽입 기능
- 최종 Shorts MP4 생성
- 네이버 블로그 TXT 및 선택 사진 출력

### 절대 금지

- 전체 프로그램을 새로 작성하지 않는다.
- 기존 Gemini 프롬프트를 임의로 변경하지 않는다.
- Edge-TTS를 다른 TTS로 교체하지 않는다.
- 현재 **2.8초 Ken Burns** 값을 변경하지 않는다.
- 기존 무대 영상 2개 자동 삽입 기능을 삭제하지 않는다.
- 기존 이미지 수집/선별 UI를 없애지 않는다.
- 기존 썸네일 편집 기능을 없애지 않는다.
- 새 B-roll 기능 때문에 기존 영상 제작이 실패해서는 안 된다.

---

# 2. 현재 프로그램의 실제 흐름

현재 시스템은 다음 구조다.

```text
가수 뉴스 검색
   ↓
기사 목록 표시
   ↓
기사 선택
   ↓
기사 URL 입력
   ↓
기사 자동 파싱
   ↓
Gemini API
   ├─ 썸네일 문구 5개
   ├─ 약 60초 쇼츠 대본
   └─ 네이버 블로그 4파트 글
   ↓
관련 이미지 약 30장 스마트 수집
   ↓
사용자 이미지 선별/삭제
   ↓
썸네일 선택 및 편집
   ↓
쇼츠 제작
   ├─ 선택 이미지
   ├─ 2.8초 Ken Burns
   ├─ OpenCV 얼굴 중심 크롭
   ├─ Edge-TTS
   ├─ 한 줄 굵은 자막
   └─ 기존 무대 영상 2개 자동 삽입
   ↓
최종 Shorts MP4
+
네이버 블로그 TXT + 사진
```

---

# 3. 이번 수정의 핵심 목표

현재 프로그램은 기사마다 관련 이미지를 새로 검색하기 때문에 같은 가수를 반복 제작할수록 검색/다운로드/API 호출 시간이 계속 발생한다.

이를 다음 구조로 변경한다.

```text
처음 제작
→ 검색
→ 사용자가 선택
→ 선택한 미디어를 DB에 축적

두 번째 제작
→ DB에서 먼저 검색
→ 부족한 것만 외부 검색

세 번째 제작
→ DB 활용률 증가
→ 외부 검색 감소

장기적으로
→ 가수별 미디어 DB가 계속 성장
→ B-roll DB도 계속 성장
→ API 호출 감소
→ 제작시간 감소
```

즉,

> **프로그램을 사용할수록 검색하지 않아도 되는 미디어가 늘어나는 자기축적형 미디어 라이브러리**를 만든다.

---

# 4. 전체 신규 아키텍처

```text
                     ┌─────────────────────┐
                     │ 기사 + 기존 Gemini  │
                     └──────────┬──────────┘
                                ↓
                     ┌─────────────────────┐
                     │ 기존 Shorts 대본    │
                     └──────────┬──────────┘
                                ↓
                     ┌─────────────────────┐
                     │ Scene Analyzer      │
                     │ (후처리 분석)       │
                     └──────────┬──────────┘
                                ↓
               ┌────────────────────────────────┐
               │ 장면별 필요한 미디어 판단     │
               └───────────────┬────────────────┘
                               ↓
              ┌────────────────────────────────┐
              │ 1순위: Singer Media DB         │
              │ 2순위: General B-roll DB       │
              │ 3순위: 외부 API 검색            │
              └───────────────┬────────────────┘
                              ↓
                   ┌────────────────────┐
                   │ 선택/저장/축적      │
                   └─────────┬──────────┘
                             ↓
                   ┌────────────────────┐
                   │ 기존 Video Engine  │
                   └────────────────────┘
```

---

# 5. 미디어 저장 구조

실제 파일과 검색 DB를 분리해서 관리한다.

권장 구조:

```text
MEDIA_LIBRARY/
│
├─ SINGER_MEDIA/
│  ├─ 임영웅/
│  │  ├─ photos/
│  │  └─ stage_clips/
│  │
│  ├─ 영탁/
│  │  ├─ photos/
│  │  └─ stage_clips/
│  │
│  └─ ...
│
├─ GENERAL_BROLL/
│  ├─ money/
│  ├─ smartphone/
│  ├─ office/
│  ├─ hospital/
│  ├─ family/
│  ├─ concert/
│  ├─ audience/
│  ├─ city/
│  ├─ travel/
│  └─ ...
│
├─ API_CACHE/
│  ├─ pexels/
│  └─ pixabay/
│
└─ projects/
   └─ project_id/
```

실제 디렉터리명은 기존 프로젝트 구조와 충돌하지 않는 범위에서 Antigravity가 결정한다.

---

# 6. SQLite 미디어 DB 구축

파일만 폴더에 저장하지 말고 **SQLite를 메인 인덱스**로 사용한다.

권장 DB:

```text
media_library.db
```

## 6-1. 기본 테이블

### singers

```text
id
name
aliases
created_at
```

### media

```text
id
singer_id
media_type
subtype
file_path
file_hash
source
source_url
provider_media_id
author
license
description
created_at
last_used_at
use_count
favorite
active
```

### media_tags

```text
media_id
tag
```

### api_cache

```text
id
provider
normalized_query
media_type
response_json
created_at
expires_at
```

### projects

```text
id
article_url
singer
created_at
```

### project_media

```text
project_id
media_id
scene_index
role
```

필요하면 검색어 alias/동의어 테이블을 추가한다.

---

# 7. 미디어 메타데이터

선택하여 라이브러리에 저장하는 미디어는 가능한 한 다음 정보를 함께 저장한다.

- 가수명
- 미디어 종류
- 세부 유형
- 파일 경로
- 파일 hash
- 출처
- 원본 URL
- provider
- provider media ID
- 제작자/저작자 정보
- 라이선스 정보
- 태그
- 설명
- 최초 저장일
- 마지막 사용일
- 사용 횟수
- 즐겨찾기 여부
- 활성/비활성 상태

### 중요

이 DB는 **저작권을 자동으로 해결해 주는 DB가 아니다.**

특히 웹에서 수집한 연예인 사진은 DB에 저장했다고 해서 사용 권리가 생기는 것이 아니다.

따라서 출처와 원본 URL을 최대한 보존한다.

---

# 8. 가수 사진 DB의 동작 방식

## 기존 방식

기사마다:

```text
외부 검색
→ 약 30장 수집
→ 사용자 선별
→ 영상 제작
```

## 변경 방식

```text
기사
 ↓
가수명 추출
 ↓
가수 Media DB 검색
 ↓
관련 사진 우선 사용
 ↓
부족한 경우에만 외부 검색
 ↓
사용자가 최종 선별
 ↓
최종적으로 실제 사용/선택된 사진을 DB에 등록
```

### 핵심

**사용자가 삭제한 사진은 재사용 DB에 넣지 않는다.**

사용자가 최종적으로 선택한 사진만 라이브러리의 재사용 후보로 축적한다.

---

# 9. 스마트 랜덤 선택

DB에서 무조건 랜덤으로 뽑지 않는다.

다음 요소를 조합한다.

```text
관련성
+
최근 사용 여부
+
사용 횟수
+
즐겨찾기
+
중복 여부
+
현재 기사와의 적합성
```

예:

```text
관련성 높은 사진      가중치 높음
오래 사용하지 않은 사진  가중치 높음
사용 횟수 많은 사진      가중치 낮음
최근 프로젝트 사용 사진   가중치 낮음
즐겨찾기 사진            가중치 높음
동일 이미지              제외
```

목표는 **자연스러운 다양성**이다.

---

# 10. 동일 프로젝트 중복 방지

한 영상 안에서 동일한 사진/B-roll이 반복되지 않도록 한다.

최소한 다음을 적용한다.

```text
file_hash 중복 검사
+
project_media 중복 검사
+
최근 사용 media penalty
```

파일명이 달라도 동일 파일이면 hash로 중복 처리한다.

---

# 11. API 검색 구조 변경

가장 중요한 원칙:

> **DB FIRST → API FALLBACK**

외부 API를 매 장면마다 호출하지 않는다.

## 기존

```text
장면 1 → API
장면 2 → API
장면 3 → API
장면 4 → API
...
```

## 변경

```text
Scene Plan 생성
       ↓
필요한 검색어 그룹화
       ↓
Singer DB / B-roll DB 검색
       ↓
부족한 항목만 외부 검색
       ↓
선택 결과 저장
```

---

# 12. API 검색어 정규화

유사 검색어를 계속 새로 검색하지 않도록 검색어를 표준화한다.

예:

```text
"돈을 세는 모습"
"현금을 세는 사람"
"돈 세기"
"cash counting"
```

등을 내부적으로 대표 검색어/태그로 연결할 수 있도록 한다.

예:

```text
money_counting
```

이를 통해 동일하거나 유사한 B-roll을 반복 검색하는 것을 줄인다.

---

# 13. API Cache와 Media Library를 분리

두 가지를 절대 같은 개념으로 취급하지 않는다.

## API_CACHE

외부 API 검색 결과/메타데이터 저장.

```text
검색어
검색 결과
provider
검색일
response JSON
```

## MEDIA_LIBRARY

실제로 재사용할 미디어 저장.

```text
파일
파일 hash
태그
출처
라이선스
사용 횟수
마지막 사용일
```

즉,

```text
API_CACHE = 검색 결과 캐시
MEDIA_LIBRARY = 실제 재사용 자산
```

---

# 14. B-roll 시스템 구축

가수 사진과 일반 B-roll을 분리한다.

## Singer Media

```text
임영웅 사진
영탁 사진
장민호 사진
...
```

## General B-roll

```text
돈
휴대폰
직장
병원
가족
관객
공연장
도시
여행
감정 표현
뉴스
계약
문서
쇼핑
등
```

---

# 15. 기존 무대 영상과 B-roll을 분리

현재 시스템의 기존 기능:

```text
가수 무음 무대 영상 2개 자동 교차 삽입
```

이 기능은 그대로 유지한다.

새로 만드는 General B-roll과 섞지 않는다.

DB에서도 구분한다.

```text
SINGER_STAGE_CLIP
GENERAL_BROLL
SINGER_PHOTO
```

### 중요

기존 무대 영상 삽입 기능을 삭제하거나 B-roll 기능으로 대체하지 않는다.

---

# 16. Scene Analyzer 추가

기존 Gemini가 만들어 놓은 Shorts 대본을 **다시 생성하지 않는다.**

기존 Gemini 결과를 그대로 유지한 상태에서 별도의 후처리 분석을 추가한다.

```text
기존 Gemini Shorts Script
          ↓
Scene Analyzer
          ↓
장면별 시각적 필요 요소 분석
```

예:

```json
{
  "scenes": [
    {
      "scene_index": 1,
      "text": "임영웅에게 뜻밖의 소식이 전해졌습니다.",
      "visual_type": "singer_photo",
      "query": "임영웅",
      "duration": 2.8
    },
    {
      "scene_index": 2,
      "text": "팬들의 관심이 집중되고 있습니다.",
      "visual_type": "broll",
      "query": "fans concert audience",
      "duration": 2.8
    }
  ]
}
```

실제 JSON 스키마는 기존 코드에 맞춰 구현한다.

### Scene Analyzer의 역할

- 문장을 장면 단위로 분류
- 가수 사진이 적합한지 판단
- 일반 B-roll이 필요한지 판단
- 검색 키워드 생성
- 필요한 장면 수 결정
- 기존 영상 엔진이 사용할 수 있는 형태로 전달

### 절대 금지

Scene Analyzer 때문에 기존 Shorts 대본이 변경되면 안 된다.

---

# 17. B-roll 운영 방식

## 1단계: MANUAL 모드 우선

처음부터 완전 자동으로 하지 않는다.

사용자에게:

```text
[자동 B-roll]
[수동 선택]
[사용 안 함]
```

등의 선택지를 제공할 수 있도록 한다.

Scene Analyzer가 추천한 B-roll 후보를 보여주고 사용자가 선택할 수 있게 한다.

## 2단계: AUTO 모드

MANUAL 기능이 안정화된 후 자동 선택을 추가한다.

```text
DB 검색
→ 적합도 계산
→ 후보 선택
→ 자동 삽입
```

---

# 18. B-roll DB 축적 방식

사용자가 최종적으로 선택한 B-roll은 DB에 축적한다.

예:

```text
"money counting" B-roll
→ 사용자가 선택
→ GENERAL_BROLL/money/
→ DB 등록
→ tags = money, finance, cash
```

다음번:

```text
돈 관련 장면 필요
→ DB에서 먼저 검색
→ 기존 영상 사용
→ API 검색 생략
```

이 구조가 반복되면서 B-roll DB가 성장한다.

---

# 19. 외부 B-roll API

가능하면 허용된/라이선스 조건을 확인할 수 있는 소스를 우선 사용한다.

예:

```text
Pexels
Pixabay
```

단, 특정 공급자의 정책/요청 제한을 코드에 고정적으로 가정하지 말고 현재 공식 API 정책에 맞게 구현한다.

### 원칙

- 필요한 경우에만 API 호출
- 검색어 정규화
- API 결과 캐시
- 실제 선택/필요한 미디어만 다운로드
- 출처 및 라이선스 정보 저장
- 대량 무차별 다운로드 금지
- API Key 하드코딩 금지

---

# 20. 기존 이미지 수집 기능과의 통합

기존의:

```text
관련 이미지 약 30장
+
세로 우선
+
얼굴 인식 우선
+
사용자 삭제/선별
```

기능은 유지한다.

다만 소스 우선순위를 다음처럼 변경한다.

```text
① Singer Media DB
      ↓
② 부족한 만큼 기존 스마트 이미지 수집
      ↓
③ 사용자 선별
      ↓
④ 최종 선택 미디어 DB 축적
```

기존의 약 30장 수집 UX 자체를 갑자기 없애지 않는다.

---

# 21. 영상 엔진 연동

새로운 미디어 DB는 기존 영상 엔진을 교체하는 것이 아니라 **입력 소스를 확장하는 방식**으로 연결한다.

기존:

```text
사용자가 선택한 사진
→ Video Engine
```

변경:

```text
DB + 신규 검색 + 사용자 선택
→ 최종 미디어 리스트
→ 기존 Video Engine
```

---

# 22. 반드시 유지할 영상 제작 설정

다음 값/기능은 수정하지 않는다.

### Ken Burns

```text
현재 코드값: 2.8초
```

**2.8초를 그대로 유지한다.**

### TTS

```text
Edge-TTS
남성/여성
```

그대로 유지.

### 자막

```text
한 줄 굵은 자막
```

그대로 유지.

### 크롭

```text
OpenCV 얼굴 중심 스마트 크롭
9:16
```

그대로 유지.

### 기존 무대 영상

```text
가수 무음 무대 영상 2개 자동 교차 삽입
```

그대로 유지.

---

# 23. B-roll 영상 처리

General B-roll은 기본적으로:

```text
음소거
```

상태로 사용한다.

기존 TTS 음성이 중심이므로 B-roll 원본 오디오가 기존 음성을 방해하지 않도록 한다.

B-roll 역시 최종 영상 규격:

```text
9:16
```

에 맞춘다.

필요한 경우 기존 영상 처리 방식과 동일한 스마트 크롭/리사이즈 계층을 적용한다.

---

# 24. 장면 길이 처리 원칙

기존 이미지 영상의 기본 시간인 **2.8초**를 변경하지 않는다.

Scene Analyzer는 현재 영상 엔진의 타이밍 규칙을 존중한다.

즉:

```text
Scene Analyzer가 임의로
1.5초 / 2초 / 3초 등으로
기존 엔진의 시간을 변경하지 않는다.
```

필요한 경우 장면 수를 조절하거나 동일 장면 내 미디어 선택만 변경한다.

---

# 25. 실패 시 Fallback

신규 기능은 기존 제작 기능보다 우선순위가 낮다.

## Scene Analyzer 실패

```text
→ 기존 Gemini Shorts Script 그대로 사용
→ 기존 영상 제작 진행
```

## DB 검색 실패

```text
→ 기존 이미지 수집 방식 사용
```

## B-roll API 실패

```text
→ B-roll 생략
→ 기존 가수 사진/무대 영상으로 진행
```

## JSON 파싱 실패

```text
→ 오류 로그 기록
→ 기존 영상 제작 흐름 유지
```

### 핵심

> **B-roll 때문에 전체 Shorts 생성이 실패하면 안 된다.**

---

# 26. UI 추가 요구사항

기존 UI를 깨지 않는 범위에서 다음 기능을 추가한다.

## Media Library

사용자가:

- 가수별 사진 확인
- B-roll 확인
- 태그 확인
- 사용 횟수 확인
- 마지막 사용일 확인
- 즐겨찾기
- 비활성화
- 삭제/관리

할 수 있도록 확장한다.

## B-roll 후보

Scene Analyzer가 추천한 B-roll 후보를:

```text
미리보기
태그
출처
```

와 함께 보여준다.

## 모드

```text
MANUAL
AUTO
OFF
```

선택이 가능하도록 한다.

---

# 27. 제작 속도 개선 목표

단순히 기능을 추가하는 것이 아니라 **반복 제작 시간이 실제로 줄어드는지** 확인한다.

프로그램에 다음 통계를 표시할 수 있도록 한다.

```text
이번 제작:
DB 재사용 미디어: 14개
외부 검색 미디어: 6개
API 호출: 2회
캐시 사용: 4회
전체 미디어: 20개
```

장기적으로:

```text
DB 재사용률 ↑
API 호출 ↓
외부 검색 ↓
제작 시간 ↓
```

가 되는 것을 목표로 한다.

---

# 28. 추천 개발 순서

한 번에 모든 기능을 넣지 않는다.

## Phase 0 — 현재 코드 보호

- 현재 프로젝트 전체 백업
- Git 또는 버전 폴더 생성
- 현재 실행 가능한 상태 확인
- 주요 함수/파일 구조 파악
- 기존 테스트 영상 1개 보존

## Phase 1 — SQLite Media DB

- DB 생성
- singer/media/tag 테이블 구축
- 파일 hash 중복 검사
- 기존 사용 이미지 등록 기능 추가

## Phase 2 — Singer Media DB

- 가수별 폴더
- 기존 선택 이미지 저장
- DB 등록
- 다음 제작에서 DB 우선 검색
- 부족하면 기존 외부 이미지 수집

## Phase 3 — B-roll DB

- GENERAL_BROLL 구조
- 태그 시스템
- 수동 등록
- API Cache 구축

## Phase 4 — Scene Analyzer

- 기존 Shorts Script 후처리
- JSON 생성
- 장면/검색어 분류
- 기존 Gemini 결과 불변

## Phase 5 — Manual B-roll

- 후보 표시
- 미리보기
- 사용자 선택
- 영상에 삽입

## Phase 6 — Video Engine 연동

- 기존 2.8초 유지
- Edge-TTS 유지
- 자막 유지
- OpenCV 유지
- 기존 무대 영상 2개 유지
- B-roll을 추가 레이어로 연결

## Phase 7 — AUTO B-roll

- 적합도 계산
- 자동 선택
- 중복 방지
- 최근 사용 penalty
- DB 우선 선택

## Phase 8 — 성능/관리 화면

- DB 재사용률
- API 호출량
- 외부 검색량
- 저장 미디어 수
- 가수별 DB 크기
- B-roll 카테고리별 통계

---

# 29. 테스트 요구사항

최소 다음 시나리오를 테스트한다.

## Test 1

처음 제작하는 가수.

```text
DB 없음
→ 외부 검색
→ 사용자 선택
→ DB 축적
→ 정상 영상 생성
```

## Test 2

동일 가수 두 번째 제작.

```text
DB 존재
→ DB 우선
→ 부족분만 외부 검색
→ 정상 영상 생성
```

## Test 3

동일 B-roll 반복 요청.

```text
DB 존재
→ API 호출 없이 재사용
```

## Test 4

Scene Analyzer 실패.

```text
→ 기존 영상 제작 정상 완료
```

## Test 5

외부 B-roll API 실패.

```text
→ B-roll 없이 기존 영상 정상 완료
```

## Test 6

동일 사진 중복.

```text
→ hash 검사
→ 동일 이미지 제외
```

## Test 7

기존 기능 회귀 테스트.

```text
기사 선택
→ Gemini
→ 썸네일
→ 이미지 선별
→ 영상
→ Edge-TTS
→ 자막
→ MP4
```

전체가 기존과 동일하게 작동해야 한다.

---

# 30. 성능상 가장 중요한 원칙

다음 구조를 반드시 지킨다.

```text
[DB에 충분한 자료 있음]
        ↓
외부 API 호출하지 않음

[DB에 자료 부족]
        ↓
부족한 만큼만 외부 검색

[외부 검색]
        ↓
검색 결과 캐시

[실제 사용]
        ↓
Media Library 축적
```

따라서 같은 검색을 계속 반복하지 않는다.

---

# 31. 저작권/운영 관련 주의

이 시스템의 Media DB는 **저작권 보증 시스템이 아니다.**

특히:

- 웹에서 수집한 연예인 사진
- 뉴스 기사에 포함된 사진
- 공연 영상
- 방송 캡처

등은 출처를 저장한다고 해서 자유롭게 상업 이용할 수 있는 것은 아니다.

또한:

> 사진 사이에 짧은 B-roll을 넣는 것만으로 저작권 문제가 자동 해결되는 것은 아니다.

따라서 이 수정의 목적은:

```text
검색 속도 개선
+
미디어 축적
+
제작 자동화
+
반복 작업 감소
```

이지,

```text
저작권 안전 보장
수익화 보장
```

이 아니다.

---

# 32. 개발자가 임의로 하지 말아야 할 것

Antigravity는 다음 작업을 임의로 수행하지 않는다.

1. 프로젝트 전체 재작성
2. 기존 Gemini 프롬프트 전면 수정
3. 기존 Gemini 결과 JSON 구조 강제 변경
4. Edge-TTS 제거/교체
5. Ken Burns 2.8초 변경
6. 기존 무대 영상 2개 기능 제거
7. 기존 이미지 선별 UI 제거
8. 썸네일 기능 변경
9. 모든 장면마다 API 호출
10. 대량 이미지/영상 무차별 다운로드
11. API Key 소스코드 하드코딩
12. B-roll 오류 때문에 전체 영상 제작 중단
13. 기존 기능과 신규 기능을 한꺼번에 대규모 리팩터링
14. 사용자 확인 없이 기존 데이터 삭제

---

# 33. 구현 전 반드시 먼저 할 일

Antigravity는 코드를 바로 수정하지 말고 먼저:

### STEP 1

현재 프로젝트 구조를 분석한다.

### STEP 2

다음 항목의 실제 코드 위치를 확인한다.

```text
기사 검색
기사 파싱
Gemini 호출
Gemini 결과 처리
이미지 수집
이미지 선별 UI
썸네일 생성
영상 생성
Ken Burns
OpenCV crop
Edge-TTS
자막
무대 영상 삽입
최종 MP4 출력
```

### STEP 3

각 기능이 어떤 함수/파일에 있는지 개발자에게 보고한다.

### STEP 4

신규 기능이 들어갈 최소 변경 지점을 제시한다.

### STEP 5

그 후 단계별로 수정한다.

---

# 34. 최종 성공 기준

다음 조건을 모두 만족해야 한다.

### 기존 기능

- 기존 기사 → Gemini → 이미지 → 썸네일 → Shorts → 블로그 흐름 정상
- 기존 Edge-TTS 정상
- 기존 자막 정상
- 기존 OpenCV crop 정상
- 기존 2.8초 Ken Burns 정상
- 기존 무대 영상 2개 정상
- 기존 MP4 출력 정상

### 신규 기능

- 선택한 가수 사진이 DB에 축적됨
- 동일 가수 재제작 시 DB 사진 우선 사용
- 부족한 경우에만 외부 검색
- B-roll DB가 축적됨
- B-roll도 DB 우선 사용
- API 검색 결과가 캐시됨
- 동일 미디어 중복 방지
- Scene Analyzer가 기존 Shorts Script를 변경하지 않음
- B-roll 실패 시 기존 영상 제작이 계속됨

### 최종 목표

```text
프로그램을 많이 사용할수록

가수별 사진 DB ↑
B-roll DB ↑
DB 재사용률 ↑
API 호출 ↓
외부 검색 ↓
제작시간 ↓
```

이 구조가 실제로 작동해야 한다.

---

# 35. Antigravity 최종 작업 지시

> **이 문서를 전체 프로젝트 재작성 요구서로 해석하지 말 것.**
>
> 현재 정상 작동하는 시스템을 기준으로 **점진적인 기능 확장**을 수행한다.
>
> 가장 중요한 원칙은:
>
> **기존 기능 보존 → Media DB 구축 → DB 우선 검색 → 부족분만 API → Scene Analyzer → B-roll → 기존 Video Engine 연동**
>
> 순서다.
>
> 특히 현재 코드의 **2.8초 Ken Burns, Edge-TTS, 한 줄 굵은 자막, OpenCV 얼굴 중심 크롭, 기존 무대 영상 2개 자동 삽입 기능은 그대로 유지**한다.
>
> Gemini가 현재 생성하는 썸네일 5개, Shorts Script, 블로그 4파트 결과도 그대로 유지한다.
>
> Scene Analyzer는 기존 결과를 변경하는 것이 아니라 **기존 Shorts Script를 입력으로 받아 시각적 장면 정보를 추가 생성하는 후처리 모듈**로 구현한다.
>
> 신규 기능의 오류가 기존 Shorts 제작을 막아서는 안 된다.
>
> 구현은 반드시 Phase 0부터 단계적으로 진행하고, 각 Phase 완료 후 기존 기능 회귀 테스트를 수행한다.
>
> 최종적으로 사용자가 프로그램을 사용할수록 가수별 사진과 B-roll이 축적되어 외부 검색과 API 호출이 줄어드는 **자기축적형 미디어 라이브러리 시스템**을 완성한다.
