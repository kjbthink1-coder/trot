# Google Antigravity 신규 기능 추가 수정 지시서
## 기존 트롯 쇼츠 자동화 시스템 + 가수별 CC 영상 수집·컷편집·DB 축적 기능

### 1. 작업 목적

현재 구현된 **가수 사진 수집 → 화면 표시 → 사용자가 삭제/선택 → 이미지 크기 조절 및 편집 → DB 축적** 기능은 그대로 유지한다.

여기에 신규로 **해당 가수의 YouTube Creative Commons(CC) 영상도 사진과 함께 수집하고, 같은 화면에서 사용자가 직접 필요한 구간만 3~4초 정도로 컷 편집한 뒤 가수별 영상 DB에 저장하는 기능**을 추가한다.

핵심은 **CC 영상을 일반 B-roll로 취급하지 않는 것**이다.

- 일반 B-roll = 장소, 꽃, 관객, 무대 분위기 등 일반적인 보조 영상
- 가수 CC 영상 = 실제 해당 가수가 등장하는 영상
- 두 미디어는 DB와 사용 목적을 명확하게 분리한다.

---

# 2. 기존 기능 절대 변경 금지

현재 정상 작동하는 기능을 재작성하지 않는다.

특히 다음 기능은 그대로 유지한다.

- 기사 URL 입력
- 기사 파싱
- 가수명 추출
- Gemini 대본 생성
- 블로그 생성
- 가수 관련 이미지 약 30장 수집
- 이미지 화면 표시
- 사용자가 사진 삭제/선택
- 이미지 크기 조절/편집
- 썸네일 지정
- 기존 사진 DB
- 기존 일반 B-roll DB
- 기존 Pexels B-roll 기능
- 기존 가수 무대 영상 기능
- Edge-TTS
- 기존 자막
- 기존 2.8초 Ken Burns
- 기존 OpenCV 얼굴 중심 크롭
- 기존 Shorts 렌더링

현재 시스템은 이미 미디어 DB를 중심으로 축적·재사용하는 구조를 갖고 있으므로, 신규 CC 영상 기능도 이 구조를 확장하는 방식으로 구현한다.

---

# 3. 신규 기능의 핵심 구조

기존:

```text
가수 기사
 ↓
가수명 추출
 ↓
가수 사진 약 30장 수집
 ↓
사용자가 사진 선택/삭제/편집
 ↓
사진 DB 축적
```

변경:

```text
가수 기사
 ↓
가수명 추출
 ↓
 ┌───────────────────────┐
 │                       │
 ↓                       ↓
가수 사진 약 30장       가수 CC 영상 약 5개
 │                       │
 ↓                       ↓
사진 선택/삭제/편집      영상 미리보기
 │                       │
 │                       ↓
 │                  시작/종료 구간 선택
 │                       │
 │                       ↓
 │                  3~4초 클립 생성
 │                       │
 └───────────┬───────────┘
             ↓
      최종 선택 미디어
             ↓
      가수별 Media DB 저장
```

---

# 4. 사진과 CC 영상은 같은 화면에서 관리

현재 사진 수집 후 화면에 사진을 뿌려주는 UI를 최대한 활용한다.

화면을 다음과 같이 확장한다.

```text
[ 가수명 ]

━━━━━━━━━━━━━━━━━━━━━━━━━━
📷 사진 수집 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━

[사진1] [사진2] [사진3] ... 약 30장

☑ 선택
🗑 삭제
✂ 사진 편집
📐 크기 조절

━━━━━━━━━━━━━━━━━━━━━━━━━━
🎬 해당 가수 CC 영상
━━━━━━━━━━━━━━━━━━━━━━━━━━

[영상1] [영상2] [영상3] [영상4] [영상5]

▶ 미리보기

시작: 00:12.0
종료: 00:15.8

[구간 선택/미리보기]

[✂ 3.8초 클립 만들기]

[저장]
━━━━━━━━━━━━━━━━━━━━━━━━━━

[최종 선택 저장]
```

**중요:** 별도의 복잡한 영상 편집 프로그램을 만드는 것이 아니다.

이번 단계에서는 다음 정도만 구현한다.

### 영상 컷 편집 기능

- 영상 재생
- 시작 시간 지정
- 종료 시간 지정
- 구간 미리보기
- 선택 구간 자르기
- 3~4초 정도의 짧은 클립 생성
- 필요하면 9:16 규격으로 변환
- 최종 파일 저장
- DB 등록

트랜지션, 자막, 효과, 색보정, 다중 트랙 등의 전문 영상 편집 기능은 이번 작업에서 구현하지 않는다.

---

# 5. CC 영상 수집량

가수별 영상 검색 시 기본적으로:

```text
사진 약 30장
+
CC 영상 약 5개
```

를 함께 확보한다.

단, 정확히 5개를 반드시 채우기 위해 무리해서 부적합한 영상을 가져오지 않는다.

적합한 CC 영상이 3개밖에 없다면 3개만 표시한다.

---

# 6. YouTube CC 영상 검색

가수명을 기준으로 검색한다.

예:

```text
임영웅
```

검색 결과에서 **Creative Commons 라이선스 영상**을 우선적으로 가져온다.

가능하면 YouTube Data API의 Creative Commons 검색 조건을 사용한다.

```text
가수명
+
videoLicense=creativeCommon
```

검색 결과에는 최소한 다음 메타데이터를 보존한다.

```text
singer_id
singer_name
youtube_video_id
youtube_url
title
channel_name
published_at
license
source
downloaded_at
original_duration
```

---

# 7. 가장 중요한 부분 — 원본 영상을 그대로 DB에 쌓지 않는다

사용자가 영상을 확인한 뒤 **필요한 부분만 잘라서 저장**한다.

예:

```text
원본 YouTube 영상
00:00 ───────────────── 03:20

사용자가 선택
        ↓

00:42.3 ───── 00:46.0
              3.7초

        ↓

최종 DB 저장
singer_cc_0001.mp4
```

이렇게 하면:

- 저장 용량 감소
- 영상 DB 관리 용이
- 다음 제작 시 즉시 사용 가능
- 다시 원본을 다운로드할 필요 감소
- 다음 제작 때 다시 컷 편집할 필요 감소

라는 효과가 있다.

---

# 8. 가수별 CC 영상 DB를 별도로 관리

일반 B-roll DB와 혼합하지 않는다.

예:

```text
media
 ├── singer_photo
 ├── singer_cc_video
 └── generic_broll
```

가능하면 기존 `media` 테이블의 media_type 확장 방식으로 구현하고, 현재 DB 구조를 불필요하게 재설계하지 않는다.

예:

```text
media_type =
    singer_photo
    singer_cc_video
    generic_broll
```

그리고 `singer_cc_video`는 반드시 특정 `singer_id`와 연결한다.

---

# 9. CC 영상 DB에 저장할 정보

최소한 다음 정보를 저장한다.

```text
id
singer_id
media_type
file_path

youtube_video_id
youtube_url
source
channel_name
video_title

original_start
original_end

clip_duration

width
height
fps

file_hash
phash 또는 유사 중복 검사값

license
license_checked_at

downloaded_at
created_at

use_count
last_used_at

rights_status
```

`rights_status` 예:

```text
verified
needs_review
unknown
```

---

# 10. 저작권/CC 관련 주의

**Creative Commons라고 검색되었다는 이유만으로 해당 영상의 상업적 이용 권리가 자동으로 보장된다고 판단하지 않는다.**

따라서 시스템은 반드시 원본 URL, 영상 ID, 채널명, 라이선스 정보 등을 보존한다.

DB에 저장하는 것은 **권리 해결 기능이 아니라 출처 및 라이선스 정보 관리 기능**이다.

기존 사진 DB 역시 저장 자체가 사용권을 발생시키는 구조가 아니므로 출처와 원본 URL을 보존하도록 되어 있다. 이 원칙을 CC 영상에도 동일하게 적용한다.

---

# 11. DB FIRST 구조

가수별 CC 영상도 사진과 동일하게 **DB FIRST → 부족할 때 외부 검색** 구조로 만든다.

예:

```text
임영웅 쇼츠 제작
       ↓
임영웅 singer_cc_video DB 검색
       ↓
사용 가능한 영상 충분
       ↓
DB 영상 사용
```

부족하면:

```text
임영웅 singer_cc_video DB 검색
       ↓
부족
       ↓
YouTube CC 검색
       ↓
약 5개 후보 확보
       ↓
사용자 확인
       ↓
필요 구간 컷
       ↓
가수 CC 영상 DB 추가
```

기존 사진 시스템도 가수 Media DB를 먼저 검색하고 부족한 경우에만 외부 검색하는 구조이므로 동일한 개념을 적용한다.

---

# 12. 사용자가 선택할 수 있는 Shorts 제작 모드

최종 Shorts 제작 화면에 다음 선택지를 추가한다.

### MODE 1
```text
📷 가수 사진만
```

기존 방식.

### MODE 2
```text
🎬 가수 CC 영상만
```

가수별 CC 영상 DB에서 짧은 클립을 사용.

### MODE 3
```text
📷 + 🎬 가수 사진 + 가수 CC 영상
```

기본적으로 가장 중요한 신규 모드.

사진 사이에 해당 가수의 실제 짧은 영상이 들어간다.

### MODE 4
```text
📷 + 🎬 + B-roll
가수 사진 + 가수 CC 영상 + 일반 B-roll
```

사용자가 원하는 경우에만 일반 B-roll도 추가한다.

---

# 13. 영상 몰입도 우선 원칙

가수 CC 영상은 **일반 B-roll처럼 자동 삽입하지 않는다.**

예:

```text
임영웅 사진
 ↓
임영웅 사진
 ↓
임영웅 실제 CC 영상 3.5초
 ↓
임영웅 사진
 ↓
임영웅 사진
```

이런 구조를 목표로 한다.

반대로:

```text
임영웅 사진
 ↓
꽃 영상
 ↓
임영웅 사진
 ↓
공연장 영상
```

처럼 사용자가 선택하지 않은 일반 B-roll이 갑자기 들어가서 영상의 몰입을 깨는 구조는 피한다.

---

# 14. 일반 B-roll과 완전 분리

기존 B-roll 시스템은 그대로 유지한다.

```text
GENERIC B-ROLL
꽃
관객
공연장
조명
도시
여행
분위기
등
```

신규:

```text
SINGER CC VIDEO
임영웅
영탁
장민호
이찬원
정동원
등
```

두 종류를 서로 혼동하지 않는다.

---

# 15. 기존 영상 엔진 연동

현재 Video Engine을 전면 재작성하지 않는다.

기존:

```text
사진
→ 2.8초 Ken Burns
→ TTS
→ 자막
→ 영상
```

구조를 유지하면서 `singer_cc_video`라는 새로운 미디어 타입을 추가한다.

CC 영상은 이미 필요한 구간을 잘라놓은 짧은 영상이므로 해당 구간 자체를 타임라인의 하나의 미디어 클립으로 취급한다.

예:

```text
PHOTO 2.8초
PHOTO 2.8초
SINGER_CC 3.7초
PHOTO 2.8초
PHOTO 2.8초
SINGER_CC 3.2초
PHOTO 2.8초
```

최종 60초 영상에 맞춰 기존 렌더링 파이프라인에 연결한다.

기존 2.8초 Ken Burns, Edge-TTS, 자막, OpenCV 크롭 등의 기존 기능은 변경하지 않는다.

---

# 16. 영상 파일 정규화

DB에 저장하는 최종 CC 클립은 재사용을 쉽게 하기 위해 동일한 규격으로 정규화한다.

권장:

```text
MP4
H.264
AAC 또는 기존 시스템 호환 음성 설정
1080x1920
30fps
3~4초
```

단, 기존 Video Engine의 실제 호환 규격이 있다면 **기존 규격을 우선한다.**

---

# 17. 원본 음성 처리

이번 기능에서는 CC 영상의 원본 음성을 Shorts에서 반드시 사용할 필요는 없다.

기본값은:

```text
CC 영상 원본 음성 제거/무음화
```

를 우선 검토한다.

기존 Shorts의

```text
TTS
+
배경음/효과음
+
자막
```

구조와 충돌하지 않도록 한다.

단, 기존 시스템에서 영상 원본 음성을 활용하는 기능이 이미 있다면 그것을 깨뜨리지 말고 옵션으로 처리한다.

---

# 18. 중복 방지

같은 영상을 반복 저장하지 않는다.

최소:

```text
youtube_video_id
+
original_start
+
original_end
+
file_hash
```

를 이용한다.

가능하면 이미지 DB에 적용 예정인 perceptual hash 방식도 영상 썸네일/대표 프레임 또는 영상 fingerprint 방식으로 확장할 수 있다.

---

# 19. UI에서 반드시 확인할 것

사용자가 저장 버튼을 누르기 전에 다음 정보를 볼 수 있어야 한다.

```text
[임영웅 CC 영상 #1]

원본:
YouTube 제목

구간:
00:12.4 ~ 00:16.1

길이:
3.7초

[▶ 미리보기]

[다시 자르기]

[☑ DB 저장]
```

사용자가 원하지 않는 영상은 저장하지 않는다.

사진 시스템의 원칙과 마찬가지로 **사용자가 삭제/제외한 미디어는 재사용 DB에 등록하지 않는다.**

---

# 20. 최종 UX 목표

사용자가 프로그램을 사용할 때:

```text
① 기사 입력
        ↓
② 가수 자동 인식
        ↓
③ 사진 약 30장 + CC 영상 약 5개 자동 수집
        ↓
④ 한 화면에서 사진/영상 확인
        ↓
⑤ 사진 삭제/선택/편집
        ↓
⑥ CC 영상 재생
        ↓
⑦ 필요한 구간만 3~4초로 자르기
        ↓
⑧ 최종 선택
        ↓
⑨ 사진 DB + 가수 CC 영상 DB에 축적
        ↓
⑩ Shorts 제작
```

이 과정을 목표로 한다.

---

# 21. 장기적으로 기대하는 구조

프로그램을 사용할수록:

```text
가수별 사진 DB ↑
가수별 CC 영상 DB ↑
일반 B-roll DB ↑

        ↓

DB 재사용률 ↑
외부 검색 ↓
YouTube 검색/다운로드 ↓
영상 컷편집 반복 ↓
제작시간 ↓
저장공간 효율 ↑
```

즉, **처음에는 조금 작업하더라도 사용할수록 가수별 미디어 라이브러리가 축적되어 다음 제작이 빨라지는 구조**를 만든다.

---

# 22. 절대 하지 말아야 할 것

1. 기존 Video Engine 전체 재작성 금지
2. 기존 사진 수집 기능 변경 금지
3. 기존 사진 삭제/편집 UI 제거 금지
4. 기존 일반 B-roll 시스템과 CC 영상을 하나의 카테고리로 통합 금지
5. CC 영상을 무조건 자동으로 Shorts에 삽입하지 말 것
6. 사용자가 선택하지 않은 CC 영상을 DB에 저장하지 말 것
7. 원본 YouTube 메타데이터를 버리지 말 것
8. CC 라이선스를 이유로 저작권이 해결됐다고 판정하지 말 것
9. 전문 영상 편집 프로그램 수준의 기능을 이번 단계에서 만들지 말 것
10. 신규 기능 오류 때문에 기존 사진 기반 Shorts 제작이 중단되지 않도록 Fail-safe 처리할 것

---

# 23. 구현 우선순위

### Phase 1
YouTube CC 검색 → 약 5개 후보 → 화면 표시

### Phase 2
영상 미리보기 → 시작/종료 구간 지정 → 3~4초 컷 생성

### Phase 3
컷 영상 9:16 정규화 → 저장

### Phase 4
가수별 `singer_cc_video` DB 축적

### Phase 5
DB FIRST → 부족할 때만 YouTube CC 검색

### Phase 6
Shorts 제작 화면에 선택 모드 추가

```text
[ ] 사진만
[ ] 가수 CC 영상만
[ ] 사진 + 가수 CC 영상
[ ] 사진 + 가수 CC 영상 + 일반 B-roll
```

### Phase 7
기존 Video Engine과 연동 및 회귀 테스트

---

# 24. 최종 개발 판단 기준

이번 작업의 성공 기준은 단순히 "YouTube 영상을 다운로드할 수 있다"가 아니다.

다음이 모두 가능해야 한다.

```text
✓ 가수별 CC 영상 검색
✓ 약 5개 후보를 기존 사진 화면과 함께 표시
✓ 사용자가 영상 확인
✓ 원하는 구간 지정
✓ 3~4초 클립으로 즉시 컷
✓ 필요한 영상만 최종 저장
✓ 가수별 DB에 축적
✓ 다음 제작에서 DB 우선 재사용
✓ 사진과 CC 영상 혼합 제작
✓ 일반 B-roll과 선택적으로 병행
✓ 기존 사진 기반 제작 기능 유지
✓ 신규 기능 오류가 기존 제작을 방해하지 않음
```

**핵심 개념을 다시 강조한다.**

> **CC 영상은 B-roll이 아니다.**
>
> **"해당 가수의 실제 영상 클립을 해당 가수의 사진 DB와 같은 방식으로 가수별 라이브러리에 축적하고, 사용자가 필요할 때 사진 사이에 선택적으로 삽입하는 '가수 전용 영상 미디어 자산'이다.**

이 개념을 기준으로 기존 코드를 최대한 재사용하여 단계적으로 구현하라.